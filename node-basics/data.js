const x = 23
const y = 32

let z = 'welcome to nodejs'

let status = true

//{ c1, c2, c3} => used to export or import more number of constants / typed / named constants
module.exports = { x, y, z , status }